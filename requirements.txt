# LitServe inference server
litserve>=0.2.0
fastapi>=0.100.0

# Transcription
faster-whisper>=1.0.0

# Deep learning / speaker embeddings
torch>=2.0.0
torchaudio>=2.0.0
pyannote.audio>=3.1.0

# Audio I/O & preprocessing (ffmpeg CLI must be installed on the host)
soundfile>=0.12.0
numpy>=1.24.0

# HTTP client for callers
httpx>=0.25.0

# Schemas
pydantic>=2.0.0

# Speaker vector store (namespace per project_id)
pinecone>=5.0.0
